import chai from 'chai';
const { expect, should } = chai;

describe('Application state', function () {
  describe('state shape', function () {
    it('should conform to the specification found in the TSD file');
  });
});
