import { playerFleet } from '../../../app/transformers/api/player-fleet';
import chai from 'chai';
const { expect, should } = chai;

describe('transformers.api.playerFleet', function () {
  describe('[default]', function () {
    it('handles an invalid fleet gracefully');
    it('parses a valid fleet');
  });
});
